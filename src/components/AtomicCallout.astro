---
interface Props {
  /** URL to the Atomic GitHub repository */
  href?: string;
}

const { href = "https://github.com/flora131/atomic" } = Astro.props;
---

<div class="atomic-callout">
  <!-- Title bar with macOS-style dots -->
  <div class="atomic-titlebar">
    <div class="atomic-dots">
      <span class="dot dot-red"></span>
      <span class="dot dot-yellow"></span>
      <span class="dot dot-green"></span>
    </div>
    <span class="atomic-title">atomic</span>
  </div>

  <!-- Terminal body -->
  <div class="atomic-body">
    <div class="atomic-prompt">
      <span class="prompt-symbol">$</span>
      <span class="prompt-cmd">atomic --help</span>
    </div>
    <div class="atomic-output">
      <p>
        Currently building <strong>Atomic</strong> &mdash; an open-source tool that uses
        coding agents to help developers understand, research, and plan complex work across
        legacy, large, or greenfield codebases. Think migrations, large refactors, or
        onboarding onto a new project &mdash; with proper context management and steerability.
      </p>
      <p>
        I built this to help myself and developers stay competitive, with a tool
        dedicated to proper AI-assisted coding, human in the loop as a
        first principle, and software development lifecycle best practices. If you
        feel the same, <a href={href} target="_blank" rel="noopener noreferrer"
          >try it out</a
        >. PRs welcome.
      </p>
    </div>
    <div class="atomic-link">
      <span class="prompt-symbol">$</span>
      <a href={href} target="_blank" rel="noopener noreferrer">
        open {href.replace("https://", "")}
      </a>
    </div>
  </div>
</div>

<style>
  .atomic-callout {
    max-width: 40rem;
    margin: 0 auto;
    border: 1px solid var(--ctp-surface0, #313244);
    border-radius: 0.75rem;
    overflow: hidden;
    background: var(--ctp-mantle, #181825);
    transition:
      border-color 0.3s ease,
      background-color 0.3s ease;
  }

  .atomic-callout:hover {
    border-color: var(--ctp-surface1, #45475a);
  }

  /* Title bar */
  .atomic-titlebar {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    padding: 0.625rem 1rem;
    background: var(--ctp-base, #1e1e2e);
    border-bottom: 1px solid var(--ctp-surface0, #313244);
    transition:
      background-color 0.3s ease,
      border-color 0.3s ease;
  }

  .atomic-dots {
    display: flex;
    gap: 0.375rem;
  }

  .dot {
    width: 0.625rem;
    height: 0.625rem;
    border-radius: 50%;
  }

  .dot-red {
    background: var(--ctp-red, #f38ba8);
  }
  .dot-yellow {
    background: var(--ctp-yellow, #f9e2af);
  }
  .dot-green {
    background: var(--ctp-green, #a6e3a1);
  }

  .atomic-title {
    font-family: ui-monospace, SFMono-Regular, "SF Mono", Menlo, Consolas,
      "Liberation Mono", monospace;
    font-size: 0.75rem;
    font-weight: 500;
    color: var(--ctp-subtext0, #a6adc8);
    letter-spacing: 0.025em;
    transition: color 0.3s ease;
  }

  /* Terminal body */
  .atomic-body {
    padding: 1.25rem 1.25rem 1rem;
  }

  /* Prompt line */
  .atomic-prompt {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    margin-bottom: 1rem;
    font-family: ui-monospace, SFMono-Regular, "SF Mono", Menlo, Consolas,
      "Liberation Mono", monospace;
    font-size: 0.875rem;
  }

  .prompt-symbol {
    color: var(--ctp-green, #a6e3a1);
    font-weight: 600;
    transition: color 0.3s ease;
  }

  .prompt-cmd {
    color: var(--ctp-text, #cdd6f4);
    transition: color 0.3s ease;
  }

  /* Output text */
  .atomic-output {
    padding-left: 1.25rem;
    border-left: 2px solid var(--ctp-surface0, #313244);
    margin-bottom: 1rem;
    transition: border-color 0.3s ease;
  }

  .atomic-output p {
    font-size: 0.8125rem;
    line-height: 1.6;
    color: var(--ctp-subtext1, #bac2de);
    margin: 0;
    transition: color 0.3s ease;
  }

  .atomic-output p + p {
    margin-top: 0.625rem;
  }

  .atomic-output strong {
    color: var(--ctp-text, #cdd6f4);
    font-weight: 600;
    transition: color 0.3s ease;
  }

  .atomic-output a {
    color: var(--ctp-blue, #89b4fa);
    text-decoration: none;
    font-weight: 500;
    transition: color 0.2s ease;
  }

  .atomic-output a:hover {
    color: var(--ctp-sky, #89dceb);
    text-decoration: underline;
  }

  /* Bottom link line */
  .atomic-link {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-family: ui-monospace, SFMono-Regular, "SF Mono", Menlo, Consolas,
      "Liberation Mono", monospace;
    font-size: 0.8125rem;
  }

  .atomic-link a {
    color: var(--ctp-blue, #89b4fa);
    text-decoration: none;
    transition: color 0.2s ease;
  }

  .atomic-link a:hover {
    color: var(--ctp-sky, #89dceb);
    text-decoration: underline;
  }

  /* Light theme overrides */
  :global([data-theme="latte"]) .atomic-callout {
    border-color: var(--ctp-surface0, #ccd0da);
    background: var(--ctp-mantle, #e6e9ef);
  }

  :global([data-theme="latte"]) .atomic-callout:hover {
    border-color: var(--ctp-surface1, #bcc0cc);
  }

  :global([data-theme="latte"]) .atomic-titlebar {
    background: var(--ctp-base, #eff1f5);
    border-color: var(--ctp-surface0, #ccd0da);
  }

  :global([data-theme="latte"]) .atomic-title {
    color: var(--ctp-subtext0, #6c6f85);
  }

  :global([data-theme="latte"]) .prompt-symbol {
    color: var(--ctp-green, #40a02b);
  }

  :global([data-theme="latte"]) .prompt-cmd {
    color: var(--ctp-text, #4c4f69);
  }

  :global([data-theme="latte"]) .atomic-output {
    border-color: var(--ctp-surface0, #ccd0da);
  }

  :global([data-theme="latte"]) .atomic-output p {
    color: var(--ctp-subtext1, #5c5f77);
  }

  :global([data-theme="latte"]) .atomic-output strong {
    color: var(--ctp-text, #4c4f69);
  }

  :global([data-theme="latte"]) .atomic-output a {
    color: var(--ctp-blue, #1e66f5);
  }

  :global([data-theme="latte"]) .atomic-output a:hover {
    color: var(--ctp-sky, #04a5e5);
  }

  :global([data-theme="latte"]) .atomic-link a {
    color: var(--ctp-blue, #1e66f5);
  }

  :global([data-theme="latte"]) .atomic-link a:hover {
    color: var(--ctp-sky, #04a5e5);
  }
</style>
